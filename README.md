# BondAnalytics.App

Приложение для анализа портфеля облигаций с интеграцией Tinkoff Invest API.

## О проекте

BondAnalytics.App — это WPF-приложение на .NET 9 для анализа инвестиционного портфеля облигаций. Приложение подключается к брокерскому счёту Тинькофф через Tinkoff Invest API и предоставляет удобный интерфейс для отслеживания позиций, доходности и графиков купонных выплат.

## Основные возможности

- **Загрузка портфеля**: Автоматическая загрузка облигаций из брокерского счёта Тинькофф
- **Реальное время**: Подписка на потоковые котировки для актуальных цен
- **Фильтрация**: Фильтрация облигаций по типам (ОФЗ, корпоративные)
- **Аналитика**: Отображение ключевых метрик:
  - Текущая и средняя цена
  - Профит/убыток (с цветовой индикацией)
  - Полная стоимость с НКД
  - Купонная доходность
  - Даты ближайших купонов
- **Визуализация**: Интерактивная накопительная гистограмма купонов на год вперёд
- **Группировка**: Автоматическая группировка по типу облигаций

## Архитектура

Проект построен по принципам чистой архитектуры с разделением на три слоя:

```
BondAnalytics.App/           # Presentation layer (WPF + MVVM)
BondAnalytics.Domain/        # Domain layer (сущности, интерфейсы)
BondAnalytics.Infrastructure/ # Infrastructure (Tinkoff API, токены)
```

### Ключевые компоненты

- **Domain**:
  - `PortfolioItem` — модель позиции в портфеле
  - `BondItem` — модель облигации с купонной информацией
  - `IPortfolioService` — интерфейс для работы с портфелем
  - `ITokenProvider` — интерфейс для работы с токеном API

- **Infrastructure**:
  - `TinkoffApiService` — реализация IPortfolioService через Tinkoff Invest API
  - `EnvironmentTokenProvider` — хранение токена в переменных окружения Windows

- **App (WPF)**:
  - `MainViewModel` — основная логика, загрузка портфеля, фильтрация, подписка на цены
  - `PortfolioItemViewModel` — ViewModel для строки портфеля
  - `CouponHistogramViewModel` — построение накопительной гистограммы купонов
  - `ChartsViewModel` — управление доступными графиками

## Технологический стек

- **.NET 9** — целевая платформа
- **WPF** — пользовательский интерфейс
- **Tinkoff.InvestApi** (v0.6.18) — клиент для Tinkoff Invest API
- **OxyPlot.Wpf** (v2.2.0) — построение графиков
- **Microsoft.Extensions.DependencyInjection** — внедрение зависимостей
- **MVVM** — паттерн архитектуры UI

## Установка и настройка

### Требования

- Windows 10/11
- .NET 9 SDK или .NET 9 Runtime
- Брокерский счёт Тинькофф с доступом к API

### Настройка токена Tinkoff

1. Получите токен для Tinkoff Invest API:
   - В личном кабинете Тинькофф: https://www.tinkoff.ru/invest/
   - Настройки → API → Создать новый токен
   - Выберите права: `read` (достаточно для чтения портфеля)

2. Установите токен в переменную окружения Windows:

```cmd
setx BOND_ANALYTICS_TOKEN "ваш_токен_здесь"
```

Или через PowerShell:

```powershell
[Environment]::SetEnvironmentVariable("BOND_ANALYTICS_TOKEN", "ваш_токен_здесь", "User")
```

3. Перезапустите приложение (или перезагрузите сессию для применения переменной окружения).

### Сборка и запуск

```bash
# Клонируйте репозиторий
git clone https://github.com/yourusername/BondAnalytics.App.git
cd BondAnalytics.App

# Восстановите пакеты
dotnet restore

# Соберите проект
dotnet build -c Release

# Запустите приложение
dotnet run --project BondAnalytics.App
```

Или откройте `BondAnalytics.sln` в Visual Studio и запустите проект `BondAnalytics.App`.

## Использование

1. **Загрузка портфеля**: При запуске приложение автоматически загружает список облигаций из вашего счёта. Для обновления нажмите кнопку "Обновить".

2. **Фильтрация**: Используйте выпадающий список для фильтрации:
   - Все — все облигации
   - ОФЗ — только государственные облигации
   - Корпоративные — только корпоративные облигации

3. **Графики**: Нажмите кнопку "Графики" для открытия окна с накопительной гистограммой купонов. Гистограмма показывает ожидаемые купонные выплаты по месяцам для каждого инструмента. При наведении на столбец отображается всплывающая подсказка с деталями.

4. **Цветовая индикация**: В таблице столбец "Профит" подсвечивается:
   - Зелёный — прибыль
   - Красный — убыток

## Особенности реализации

- **Потоковые данные**: Приложение подписывается на потоковые котировки через gRPC-стрим Tinkoff API. Цены обновляются в реальном времени.
- **Накопительная гистограмма**: Строится на основе ближайших купонов в горизонте 1 года. Купоны группируются по месяцам, stacking отображает вклад каждого инструмента.
- **Безопасность**: Токен API не хранится в репозитории, используется только переменная окружения пользователя.
- **Обработка ошибок**: При отсутствии данных для гистограммы отображается информационное сообщение.

## Структура проекта

```
BondAnalytics.App/
├── App.xaml                    # Конфигурация приложения
├── App.xaml.cs                 # Настройка DI-контейнера
├── MainWindow.xaml             # Главное окно
├── MainWindow.xaml.cs
├── Converters/                 # WPF-конвертеры
│   ├── HeightScaleConverter.cs
│   └── RowIndexConverter.cs
├── ViewModels/                 # ViewModels (MVVM)
│   ├── MainViewModel.cs
│   ├── PortfolioItemViewModel.cs
│   ├── ChartsViewModel.cs
│   ├── CouponHistogramViewModel.cs
│   ├── ColorPalette.cs
│   └── RelayCommand.cs
└── Views/                      # Окна
    ├── ChartsWindow.xaml
    ├── ChartsWindow.xaml.cs
    ├── CouponHistogramWindow.xaml
    └── CouponHistogramWindow.xaml.cs

BondAnalytics.Domain/
├── BondItem.cs
├── PortfolioItem.cs
├── IPortfolioService.cs
└── ITokenProvider.cs

BondAnalytics.Infrastructure/
├── EnvironmentTokenProvider.cs
└── TinkoffApiService.cs
```

## Развёртывание

Проект настроен для публикации как single-file приложение:

```bash
dotnet publish BondAnalytics.App -c Release -r win-x64 --self-contained true /p:PublishSingleFile=true
```

Собранный исполняемый файл будет в:
```
BondAnalytics.App/bin/Release/net9.0-windows/win-x64/publish/
```

## Ограничения

- Только Windows (WPF)
- Только брокерский счёт Тинькофф
- Для работы требуется активное подключение к интернету и валидный токен API
- Приложение не является официальным продуктом Тинькофф

## Лицензия

MIT License. См. файл LICENSE для подробностей.

## Автор

Разработано с использованием .NET, WPF и Tinkoff Invest API.

---

**Примечание**: Это учебный/демонстрационный проект. Используйте на свой страх и риск. Разработчик не несёт ответственности за финансовые решения, принятые на основе данных из этого приложения.